# macOS Tauri app
macos/node_modules/
macos/dist/
macos/src-tauri/target/
macos/src-tauri/gen/
macos/.vite/
macos/dict/
macos/src-tauri/src/dict/

# Repo root leftovers from pre-monorepo
/dict/
/dist/

# iOS app
ios/MdxKit/.build/
ios/build/
ios/MdxKit/.swiftpm/xcode/xcuserdata/
ios/.dev-team

# GleanIOS.xcodeproj is 100% generated from project.yml via xcodegen (see
# ios/generate.sh) — not committed, so DEVELOPMENT_TEAM never has to be
# scrubbed before a commit. Run `cd ios && ./generate.sh` after cloning.
ios/GleanIOS.xcodeproj/

# Misc
.DS_Store
*.log
npm-debug.log*
*.pem
.env
.env.local
